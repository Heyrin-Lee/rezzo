<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.rezzo.mes.sales.oust.mapper.SalesOustMapper">
 
 	<!-- 진행중 주문서 조회 -->
 	<select id="findOrder" resultType="OrderVO">
 		SELECT ORDER_NO, VEND_NM, EDCTS_CD, PRDT_NM, ORDER_CNT
		FROM ORDER_SHEET
 	</select>
 	
 	<!-- 완제품 재고 현황 모달로 조회 -->
 	<select id="salesStcList" resultType="SalesStcVO">
 		SELECT I.EDCTS_LOT_NO, S.STC_CNT, S.MAKE_DT, DISTB_TERM
		FROM EDCTS_IST I JOIN EDCTS_STC S
		ON(#{edctsCd} = I.EDCTS_CD)
 	</select>
 </mapper>