<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rezzo.mes.sales.oust.mapper.OustMapper">

	<!-- 주문서 전체목록 -->
	<select id="orderList" resultType="OrderVO">
	SELECT ORDER_NO, VEND_NM, EDCTS_CD, PRDT_NM, ORDER_CNT
	FROM ORDER_SHEET
	</select>
	
	<!-- 완제품 재고 모달창 -->
	<select id="stcModal" resultType="EdctsVO">
	SELECT s.EDCTS_CD, s.PRDT_NM, e.SAF_STC, S.MAKE_DT, s.DISTB_TERM
	FROM EDCTS_STC s
	JOIN EDCTS e
	ON s.EDCTS_CD = #{edctsCd}
	</select>
</mapper>